

const pessoa = {   //const pessos é entidade e os valores preenchidos é "modelar/estruturar" a entidade
    name: 'Jéssica',
    idade: 30
}

// Recebemos uma estruturs orientada a objetos, e vamos manipular a forma que ele será retornado
// 