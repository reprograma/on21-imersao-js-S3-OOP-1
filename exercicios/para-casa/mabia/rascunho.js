const pessoa = {
  name: "Mabia",
  idade: 30,
};

//representação de um objeto do mundo real

class User {
  contructor(name, email, password) {
    this.name = name;
    this.email = email;
    this.password = password;
  }
}
